---
dependency:
  name: galaxy
  enabled: false
driver:
  name: vagrant
  provider:
    name: libvirt
platforms:
  - name: archlinux
    box: archlinux/archlinux
    memory: 2048
    cpus: 2
provisioner:
  name: ansible
verifier:
  name: ansible
    #lint: |
    #  set -e
    #  yamllint .
    #  ansible-lint
    #  flake8
